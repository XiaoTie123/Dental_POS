Java Spring Boot Project Deployment in Digital Ocean
================
1. Create Project on Digital Ocean
2. Create Droplets on Digital Ocean
3. Login Ubuntu Server
4. Install Java for Java Spring Boot Project
5. Install Maven
6. Install Nginx
7. Install Git
8. Git Clone Repo
9. Domain add On Digital Ocean
10. Add Nginx
11. Add SSH


you can't access permission ssh 
===========================
cd %USERPROFILE%\.ssh
notepad known_hosts
ssh-keygen -R 206.189.95.225
ssh root@206.189.95.225
yes

Install Java
============================
sudo apt update

for Java 11
===========
sudo apt install openjdk-11-jdk -y

for Java 8
==========
sudo apt install openjdk-8-jdk -y

check Java Version
==================
java -version


Install Maven
=============
sudo apt install maven

Install Nginx
=============
sudo apt update
sudo apt install nginx
sudo ufw allow 'Nginx Full'
systemctl status nginx

Install Git
===========
sudo apt install git

Git Clone
=========
git clone https://XiaoTie123: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@github.com/XiaoTie123/One_Dental_POS.git
https://github.com/XiaoTie123/Dental_POS.git
Token
=====
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

Add Nginx
=========
sudo vi /etc/nginx/sites-available/onedentalclinic.xyz

server {
    server_name onedentalclinic.xyz;
    index index.html index.htm;
    access_log /var/log/nginx/onedentalclinic.log;
    error_log  /var/log/nginx/onedentalclinic-error.log error;

    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $http_host;
        proxy_pass http://127.0.0.1:8080;
        proxy_redirect off;
    }
}

sudo ln -s /etc/nginx/sites-available/onedentalclinic.xyz /etc/nginx/sites-enabled/onedentalclinic.xyz

sudo nginx -t

sudo nginx -s reload

sh startup.sh

Add SSL Certificate using Lets Encrypt
====================================
sudo apt install snapd

sudo snap install --classic certbot

ping onedentalclinic.xyz

sudo certbot --nginx -d onedentalclinic.xyz

Git pull
========
git pull origin main
